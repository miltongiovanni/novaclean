<form method="post" action="{{ path('app_afp_delete', {'id': afp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ afp.id) }}">
    <button class="btn">Delete</button>
</form>
