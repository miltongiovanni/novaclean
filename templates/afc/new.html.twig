{% extends 'base.html.twig' %}

{% block title %}Nuevo AFC{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('parsley') }}

{% endblock %}
{% block content %}
    <h2 class="ps-3">Crear AFC</h2>

    {{ include('afc/_form.html.twig') }}

    <div class="row my-3">
        <div class="offset-4 col-2">
            <a class="btn btn-primary w-100" href="{{ path('afc_index') }}">Volver a la lista</a>
        </div>
    </div>    <a href="{{ path('afc_index') }}">back to list</a>
{% endblock %}
