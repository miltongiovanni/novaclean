<div class="dropdown">
    <a class="btn btn-primary dropdown-toggle w-100" href="#" role="button" id="dropdownMenuLink"
       data-bs-toggle="dropdown" aria-expanded="false">
        Acciones
    </a>
    <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <li>
            <a class="dropdown-item" href="{{ path('contrato_edit', {'slug': personal_contrato.slug.toRfc4122()}) }}">Editar</a>
        </li>
        <li>
            <a class="dropdown-item" href="{{ path('contrato_personal', {'slug': personal_contrato.slug.toRfc4122()}) }}">Personal contrato</a>
        </li>
    </ul>
</div>