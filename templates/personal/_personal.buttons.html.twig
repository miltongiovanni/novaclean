<div class="dropdown">
    <a class="btn btn-primary dropdown-toggle w-100" href="#" role="button" id="dropdownMenuLink"
       data-bs-toggle="dropdown" aria-expanded="false">
        Acciones
    </a>
    <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
        <li>
            <a class="dropdown-item" href="{{ path('personal_show', {'slug': empleado.slug}) }}">Ver</a>
        </li>

        {% if empleado.activo == 1 %}
            <li>
                <a class="dropdown-item" href=" {{ path('personal_deactivate', {'slug': empleado.slug}) }} ">Desactivar</a>
            </li>
        {% else %}
            <li>
                <a class="dropdown-item" href=" {{ path('personal_activate', {'slug': empleado.slug}) }} ">Activar</a>
            </li>
        {% endif %}
        <li>
            <a class="dropdown-item" href="{{ path('personal_edit', {'slug': empleado.slug}) }}">Editar</a>
        </li>
    </ul>
</div>