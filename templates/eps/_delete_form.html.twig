<form method="post" action="{{ path('app_eps_delete', {'id': ep.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ep.id) }}">
    <button class="btn">Delete</button>
</form>
